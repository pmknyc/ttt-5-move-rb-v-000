

input = gets.strip
index = input_to_index(input)
move(board, index, "O")
display_board(board)

input = gets.strip
index = input_to_index(input)
move(board, index, "X")
display_board(board)


input = gets.strip
index = input_to_index(input)
move(board, index, "O")
display_board(board)

input = gets.strip
index = input_to_index(input)
move(board, index, "X")
display_board(board)



move(board, index, "O")
move(board, index, "X")
move(board, index, "O")
move(board, index, "X")
move(board, index, "O")
move(board, index, "X")
move(board, index, "0")
move(board, index, "X")      

